# 去 AI 味：编码与 Web 排版规范

本仓库将「去 AI 味」规范拆分为两份独立、互不耦合的子规范，各自维护：

- **编码规则（codestyle）**：标识符命名混淆 —— 见 `skills/codestyle-no-ai/`
- **Web 排版规则（webuistyle）**：前端版面 / 配色 / 结构 / 代码层「去 AI 味」 —— 见 `skills/webuistyle-no-ai/`

## 总原则

看起来像「一个赶工的程序员写的能跑的页面」，而不是「模型生成的完美 demo」。逻辑必须正确、可运行，「乱」只体现在风格与排版上。
