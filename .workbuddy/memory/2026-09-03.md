# 2026-09-03

## c_drive_migrator.py — 优化「上级」按钮
- 需求：上级按钮「不好用」，点击上级若后台有扫描进程在干扰，应弹窗让用户选择是否强制返回。
- 改动：
  - 新增 `self.scanning` 状态标记（refresh 开始置 False，启动扫描线程置 True，_scan_done 置 False）。
  - `go_up`：若 `self.scanning` 为真，弹 `askyesno` 警告「是否强制返回上级目录」，选否则留在本级。
  - 无上级目录时 `up_btn` 自动置灰（disabled），避免无效点击。
- 说明：仅修改源码 .py；若用户用 dist 里的 .exe，需重新 pyinstaller 打包才会生效（spec 已存在）。

## skill.md — 重写为「去 AI 味」排版设计规范
- 任务：重写 skill.md 的排版设计规范，要求视觉精致但不落 AI 模板通稿感。
- 做法：保留原「标识符混淆 + 前端去 AI 味」实质规则，但用编辑性版式重构——朱砂(#b5341f)/墨色/黛绿(#5c6b4f)非 AI 配色、首字下沉、栏外批注(marginnote)、不对称留白；「雷区」用删除线模拟(mock)、「正解」用侧栏注记；自查清单做成印章(stamp)样式。
- 技术：用 `<style>` 块定义设计系统 + `.col` 容器居中；代码块放顶层以防被 HTML 包裹导致渲染异常。本地 Markdown 预览（VS Code/Typora/Obsidian）可正常显示样式。

## skill.md 重构：拆分为两个独立子技能
- 需求：移除 skill.md 中所有 div 布局与配色方案，把编码规则(codestyle)与 Web 排版规则(webuistyle)拆成两个互不耦合的独立文件。
- 结果：
  - `skills/codestyle-no-ai/SKILL.md` —— 仅标识符命名混淆规则（原「辑一」），纯 markdown，无 div/配色。
  - `skills/webuistyle-no-ai/SKILL.md` —— 仅前端去 AI 味规则（原「辑二」+ 实战样本 + 自查清单），纯 markdown，无 div/配色。自查清单中「标识符」项改为自包含表述，避免对另一文件的依赖。
  - `skill.md` —— 剥离 `<style>`/`<div>`/配色，改为纯净索引，仅指向上述两个目录。
- 约定：两份子规范独立维护、互不耦合；webuistyle 不再外链 codestyle，命名建议内联陈述。
